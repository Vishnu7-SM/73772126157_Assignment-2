---
- name: Print Variables
  hosts: localhost
  vars:
    - username: VISHNU
    - home: /home/VISHNU07/
  tasks:
    - name: print variables
      debug:
        msg: "Username: {{username}}, Home dir: {{home}}"

